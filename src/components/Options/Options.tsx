import React from 'react';

import { Icon } from '@gravity-ui/uikit';

import fullLogo from 'src/images/logo-full.svg';

export const Options = () => {
  return <Icon data={fullLogo} height={48} />;
};
